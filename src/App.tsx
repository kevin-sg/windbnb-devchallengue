import { AppContainer } from "@/styled-components";
import { HomePage } from "@/pages";

function App() {
  return (
    <AppContainer>
      <HomePage />
    </AppContainer>
  );
}

export default App;
